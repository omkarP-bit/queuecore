apiVersion: v1
kind: Service
metadata:
  name: queuecure-service
  namespace: queuecure
spec:
  selector:
    app: queuecure
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
